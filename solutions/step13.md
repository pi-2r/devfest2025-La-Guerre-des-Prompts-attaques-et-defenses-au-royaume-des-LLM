

XSS
task: self_check_input
```
    - should not contain code that could be used for attacks (like XSS, SQL injection)
    - should not contain HTML or JavaScript tags that could be executed
    - should not encourage unsafe web practices or security exploits
```
